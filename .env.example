# Hugging Face API token for MiniCPM-V-2 vision inference.
# Get a free token at https://huggingface.co/settings/tokens
# If set here, the token is baked into the Vite build.
# Alternatively, enter the token in the app UI — it is saved to your browser.
VITE_HF_TOKEN=hf_...
